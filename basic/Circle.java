package basic;

public class Circle {
    public static void main(String[] args){

        /*
        switch(表达式) {
            case 值1:
                语句块1;
                break;
            case 值2:
                语句块2;
                break;
            case 值n:
                语句块n;
                break;
            default:
                语句块n+1;
            break;
        }
        一定注意break
        表达式可以是byte short int char  值为整型或字符型表达式

        while 先判断条件, 再执行循环, 有可能一次都不执行
        while(){

        }

        do while 先执行循环, 再判断条件, 至少会执行一次
        do{

        }while ();

        for 循环
        首先执行表达式1进行初始化，然后判断表达式2的值，如果为true，则执行循环体语句块 否则退出循环
        最后执行表达式3，改变循环变量的值，完成一次循环，直到条件表达式 2 的值为 false，才结束循环
        int[] ns = { 1, 4, 9, 16, 25 };
        for (int i=0; i<ns.length; i++) {
            System.out.println(ns[i]);
        }
        // 无法访问i
        int n = i; // compile error!


        for each   可以直接遍历每个元素
        int[] arr = {1, 3, 5, 12, 65};
        for (int element : arr){
            System.out.println(element);
        }
        变量element直接拿到arr每个元素的值, 而不是数组的索引


        break 和 continue
        break跳出当前所在一层的循环, 这个循环不再执行
        continue提前结束本次循环, 进入下一次循环
        */
    }
}
